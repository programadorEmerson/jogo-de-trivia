import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6CA7B2",
      contrastText: "#fff",
    },
    secondary: {
      main: "#76B2BD",
      contrastText: "#fff",
    },
  },
});

export default theme;